const initialState = {
  expenseList: [],
};

const expenseReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default expenseReducer;
